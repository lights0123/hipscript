interface Window {
  wgpuDevice: GPUDevice;
  wgpuAdapter: GPUAdapter;
}
var HEAPU8: Uint8Array;
var HEAPU32: Uint32Array;
function addToLibrary(x: any): void;
function UTF8ToString(ptr: number): string;
